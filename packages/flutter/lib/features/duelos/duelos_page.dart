import 'package:flutter/material.dart';

class DuelosPage extends StatelessWidget {
  const DuelosPage({super.key});
  @override
  Widget build(BuildContext context) =>
      Scaffold(appBar: AppBar(title: const Text('DuelosPage')), body: const Center(child: Text('TODO')));
}

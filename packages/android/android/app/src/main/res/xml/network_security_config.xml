<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Cleartext permitido globalmente (APK debug; live reload usa HTTP al Vite dev server) -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
</network-security-config>
